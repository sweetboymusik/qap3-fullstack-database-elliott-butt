<body>
  <%- include('./partials/nav.ejs') %>

  <h1>Browse</h1>
  <h2>Categories!</h2>

  <div class="category-card"><h3>All</h3></div>

  <% if (categories.length > 0) { %> <% categories.forEach(category => { %>

  <div class="category-card"><h3><%= category.name %></h3></div>

  <% }) %> <% } else { %>
  <p>No categories found.</p>
  <% } %>
</body>
